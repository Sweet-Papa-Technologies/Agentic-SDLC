"use strict";
// Pre-implementation stub: loads fine, but behavior is missing.
// Tests fail on assertions (red for the RIGHT reason), not on import errors.

function slugify(_input) {
  return null;
}

module.exports = { slugify };
